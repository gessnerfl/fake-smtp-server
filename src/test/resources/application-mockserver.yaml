server:
  port: 0

management:
  server:
    port: -1

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

fakesmtp:
  port: ${random.int[1024,65536]}
  webapp:
    rate-limiting:
      whitelist-localhost: true
